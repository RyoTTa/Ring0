---
description: Show memory location, ring counts and pending ring0 proposals
---
Load the ring-memory skill. Show the current project's memory status and pending
ring0 proposals, including their text. Briefly explain how to remember or recall
something if the store is empty. If arguments specify a memory operation, handle it
according to the skill; viewing proposals does not authorize approval.

$ARGUMENTS
